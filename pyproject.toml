[tool.poetry]
name = "tdd-learning"
version = "0.1.0"
description = ""
authors = ["FionnKuo"]
readme = "README.md"
packages = [{include = "tdd_learning"}]

[tool.poetry.dependencies]
python = "^3.11"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
